# Assembly Endgame

A project made with React + Javascript, which was then converted to TypeScript + Javascript.

A classic game of hangman with the theme of saving the programming world from having to shift back to coding with Assembly. 